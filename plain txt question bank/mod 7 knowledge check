Question 1

What is the advantage of a server-side solution for displaying an XML file in required format?

A. Reduces server computing need
B. No need to write an XSL style sheet
C. Reduces client computing load
✔ D. No special requirement on Web browser

Question 2

If an XML file is created in your application, where should the file be stored in the ASP.NET application domain?

✔ A. In App_Data folder
B. In App_Code folder
C. In App_WebReferences folder
D. In Bin folder

Question 3

Which of the following terms is NOT defined in the W3C namespace http://www.w3.org/2001/XMLSchema
?

✔ A. cc:core
B. all
C. complexType
D. schema
E. choice

Question 4

A Document Type Definition (DTD) file:

✔ A. defines the syntax of an XML file.
B. defines the structure of an XML schema file.
C. extends the XML schema with more data types.
D. follows XML syntax.

Question 5

What is an empty element in XML? Select all that apply.

✔ A. An element without text content between the element tags
✔ B. An element that does not need to have an opening tag
✔ C. An element without a child element
D. An element without an attribute

Question 3

What XML processing model reads one node into memory at a time?

✔ A. SAX (Simple API for XML)
B. XMLDocument
C. DOM (Document Object Model)
D. XPath